# staratlas-cookbook-examples

To install dependencies:

```bash
bun install
```

To run:

```bash
cp ../.env.sample .env
# update .env file with your configuration
bun run runner path/to/example/index.ts
```